public static int linearSearch(int[] array, int key){
    int size = arr.length;
    for(int i=0; i<size; i++) {//starts with 0 ends with anything < size, and once complete i ++ = add 1 to i i.e. i += 1
        if(aray[i] == key){
            return i
        }
    }
return -1;
}